1 version: '3.8'
services:
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin          # Default Grafana admin username
      - GF_SECURITY_ADMIN_PASSWORD=admin      # Default Grafana admin password
      - GF_INSTALL_PLUGINS=opennms-helm-app   # Install OpenNMS Helm plugin
    volumes:
      - grafana-storage:/var/lib/grafana      # Persist Grafana data
volumes:
  grafana-storage:

2 = install plugin of open nms and enable it grom grafana
3 now go on to the data source add credentials
4 now create dashboard for the same

grafana-cli plugins update opennms-helm-app

grafana-cli plugins install opennms-opennms-app


================


version: "3.8"

networks:
  opennms-net:
    # Specify network driver if needed (optional)
    # driver: bridge

services:
  grafana:
    image: grafana/grafana:latest
    container_name: grafana-opennms
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - GF_INSTALL_PLUGINS=opennms-opennms-app # Correct plugin name
      - GF_AUTH_ANONYMOUS_ENABLED=false
      - GF_SECURITY_ADMIN_PASSWORD=yourpassword  # Set admin password
    networks:
      - opennms-net
    volumes:
      - grafana-storage:/var/lib/grafana

volumes:
  grafana-storage:


grafana-cli plugins install opennms-opennms-app inside docker or au can run compose file






