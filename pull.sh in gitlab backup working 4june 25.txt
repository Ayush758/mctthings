#!/bin/bash

# List of all target directories (edit or expand if needed)
dirs=$(find . -type d -name ".git" | sed 's|/.git||')

for dir in $dirs; do
    echo "Running: git pull in $dir"
    (
        cd "$dir" && git pull
    )
done
