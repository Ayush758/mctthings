Commands:
cd /etc/nginx
sudo mkdir /etc/nginx/ssl
cd ssl
sudo vi ssl-info.txt
---------------------------
[req]
default_bits       = 2048
prompt             = no
default_keyfile    = localhost.key
distinguished_name = dn
req_extensions     = req_ext

[dn]
C  = IN
ST = NCR
L  = Manila
O  = localhost
OU = DevOps
CN = localhost

[req_ext]
subjectAltName = @alt_names

[v3_ca]
subjectAltName = @alt_names

[alt_names]
DNS.1   = localhost
IP.1    = 127.0.0.1
IP.2    = 192.168.100.73
-----------------------------------
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout localhost.key -out localhost.crt -config ssl-info.txt
sudo mv localhost.key /etc/ssl/private/
sudo mv localhost.crt /etc/ssl/certs/


cd /etc/nginx/sites-available/
sudo mv default default.bkp


#vi /etc/nginx/sites-available/default
##
# You should look at the following URL's in order to grasp a solid understanding
# of Nginx configuration files in order to fully unleash the power of Nginx.
# https://www.nginx.com/resources/wiki/start/
# https://www.nginx.com/resources/wiki/start/topics/tutorials/config_pitfalls/
# https://wiki.debian.org/Nginx/DirectoryStructure
##
# Default server configuration
#
server {
        listen 80 default_server;
        listen [::]:80 default_server;

        # Redirect all HTTP traffic to HTTPS
        return 301 https://$host$request_uri;
}

server {
        listen 443 ssl default_server;
        listen [::]:443 ssl default_server;

        # SSL configuration
        ssl_certificate /etc/ssl/certs/localhost.crt;
        ssl_certificate_key /etc/ssl/private/localhost.key;

        # Recommended SSL settings
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;
        ssl_ciphers HIGH:!aNULL:!MD5;

        root /var/www/html;

        # Add index.php to the list if you are using PHP
        index index.html index.htm index.nginx-debian.html;

        server_name _;

        location / {
                # Enable directory listing with formatting
                autoindex on;
                autoindex_format html;
                autoindex_exact_size off;
                autoindex_localtime on;

                # Attempt to serve request as file, then as directory
                try_files $uri $uri/ =404;
        }

        error_page 404 /404.html;
        location = /404.html {
                internal;
        }

        # deny access to .htaccess files, if Apache's document root
        # concurs with nginx's one
        location ~ /\.ht {
                deny all;
        }
}

#sudo nginx -t
#systemctl restart nginx

to check ssl is applied or not 
openssl s_client -connect 13.232.120.74:443


